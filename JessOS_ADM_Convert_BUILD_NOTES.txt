================================================================================
JessOS ADM Convert - Build & Deployment Notes
================================================================================

OVERVIEW
--------
JessOS ADM Convert is a modernized replacement for Apple's original "Apple
Digital Masters Droplet" tool. It converts WAV and AIFF audio files to AAC
(.m4a) format using Apple's afconvert with Apple Digital Masters encoding
parameters (256kbps AAC, SoundCheck loudness normalization).

A single app with configurable options (right-click dock icon):
  - "Include SoundCheck Metadata" (full ADM compliance)
  - "Exclude SoundCheck Metadata (Fast)" (single-pass, faster)
  - "Output to M4A Folder" (organize output files in subfolder)


IMPROVEMENTS OVER APPLE'S ORIGINAL DROPLET
------------------------------------------
1. Apple Silicon Native
   - Runs natively on ARM64 (M1/M2/M3/M4 chips)
   - No Rosetta 2 emulation required
   - Original Apple droplet was x86_64/i386 only

2. Parallel Processing
   - Converts up to 12 files simultaneously (configurable)
   - Dramatically faster batch processing on multi-core Macs
   - Original processed files sequentially (one at a time)

3. Silent Operation
   - No popup dialogs or alerts
   - Clean, minimal progress window
   - Original had multiple alert dialogs

4. Modern Progress UI
   - Native Swift/AppKit progress window
   - Shows current file being processed
   - Purple-tinted progress bar
   - Displays conversion count in real-time

5. Streaming Architecture
   - Begins processing immediately as files are discovered
   - No waiting for full file list before starting
   - Shows activity within ~0.5 seconds of drop

6. Optimized Temp File Handling
   - CAF intermediates stored in /tmp/JessOS_ADM_Convert/
   - Automatic cleanup after conversion
   - Leverages macOS disk caching for speed

7. Network Drive Support
   - Works with files on mounted network volumes
   - No path or permission issues

8. Duplicate File Handling
   - Automatically appends -1, -2, etc. if output file exists
   - Never overwrites existing files


================================================================================
BUILD LOG (newest first)
================================================================================

--------------------------------------------------------------------------------
VERSION 3.3.2 - 2026-07-13
--------------------------------------------------------------------------------

Update-Dialog Quit Guard

Fixes:
  [+] Headless (right-click / dock-drop) conversions no longer auto-quit
      over an active Sparkle update session. The launch-time update check
      could surface an offer mid-conversion and the Keka-style quit killed
      the dialog seconds later — headless-only users could miss every
      update offer. Quits now route through AppDelegate.quitAfterConversion,
      which polls updater.sessionInProgress and terminates only after the
      update session ends (or never, if the user opened the main window).


--------------------------------------------------------------------------------
VERSION 3.3.1 - 2026-07-13
--------------------------------------------------------------------------------

Cloud Storage + Network Volume Coverage, Crash Fix

Fixes:
  [+] Right-click conversion inside File Provider domains (Dropbox, Google
      Drive, Synology Drive under ~/Library/CloudStorage): macOS never routes
      Finder Sync menus there (verified empirically — menu(for:) is not
      called even with the domain root monitored), so the app now also
      declares both actions as NSServices. They appear under the right-click
      Services submenu in cloud folders when audio files are selected.
  [+] Finder extension menu items now appear on external drives and network
      shares: monitoring "/" only covers the boot volume, so the extension
      now monitors every mounted volume (via mountedVolumeURLs, rescanned
      every 30s).
  [+] Fixed post-conversion crash: NSWindow defaults to
      isReleasedWhenClosed=true, double-releasing the headless progress
      window under ARC (segfault at autorelease-pool pop, timing-dependent).
      Both programmatic windows now set isReleasedWhenClosed=false.


--------------------------------------------------------------------------------
VERSION 3.3 - 2026-07-13
--------------------------------------------------------------------------------

Finder Right-Click Menu

New Features:
  [+] Finder Sync extension (ADMConvertFinder.appex) — "Convert to M4A - JessOS"
      and "Convert to M4A (Folder) - JessOS" appear at the top level of Finder's
      right-click menu for WAV/AIFF files and folders, with the app icon
  [+] Extension is enabled automatically on first launch (pluginkit election);
      one-time fallback prompt if that fails, plus a "Finder Extension
      Settings..." item in the app menu
  [+] admconvert:// URL scheme — the extension hands files to the main app,
      which converts headless with a per-invocation output-folder override
      (URL handled via kAEGetURL in willFinishLaunching so the main window
      never flashes open)

Removed:
  [-] Services menu entry ("Convert to M4A using JessOS") — replaced by the
      right-click menu items
  [-] Dead legacy shell-script pipeline (convert.sh, run_with_progress.sh,
      ADMProgress helper app) — all conversion has been native Swift since v3.0

Fixes:
  [+] .wave extension now accepted in headless mode (document types already
      claimed it)


--------------------------------------------------------------------------------
VERSION 3.1 - 2026-02-12
--------------------------------------------------------------------------------

Analyze Only Mode

New Features:
  [+] "Analyze Only" button — check for clipping without saving M4A files
  [+] Two action buttons when WAV/AIFF files are loaded:
      - "Analyze Only" — converts to temp, runs afclip analysis, deletes temp
      - "Convert & Analyze" — existing behavior (saves M4A + analyzes)
  [+] Drop zone now shows "WAV, AIFF, M4A supported"

Architecture:
  [+] ProcessingMode enum (.convertAndAnalyze | .analyzeOnly)
  [+] Shared convertTwoPass method eliminates code duplication
  [+] convertToTemp outputs to /tmp/ with automatic cleanup after analysis

UI:
  [+] 🍎 emoji added to Finder Services contextual menu item


--------------------------------------------------------------------------------
VERSION 3.0 - 2026-02-11
--------------------------------------------------------------------------------

Full SwiftUI Rewrite

Major Changes:
  [+] Complete rewrite using SwiftUI for modern, native interface
  [+] New file list view with real-time conversion status per file
  [+] Headless progress mode for drag-and-drop / Services workflows
  [+] Built-in auto-update system with appcast
  [+] Menu bar with preferences and manual update check

UI Improvements:
  [+] Modern SwiftUI-based content view replaces AppKit progress window
  [+] Individual file status tracking (pending, converting, complete, error)
  [+] Clean, minimal design with file type icons
  [+] Update progress window with download status

Architecture:
  [+] SwiftUI + AppKit hybrid (NSApplicationDelegate + SwiftUI views)
  [+] ConversionManager for centralized conversion state
  [+] FileConversionItem model with per-file status
  [+] UpdateManager for automatic update checking and installation
  [+] AFClip model support for advanced audio metadata

Download Page:
  [+] Landing page at jessos.com/admconvert/
  [+] Email lead capture for update notifications
  [+] Changelog page with version history


================================================================================
